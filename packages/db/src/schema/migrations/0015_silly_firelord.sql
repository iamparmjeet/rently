-- The equivalent schema change is already applied by
-- 0014_interactive_demo_sandbox. Keep this generated migration so Drizzle's
-- snapshot and migration journal remain continuous for every environment.
SELECT 1;
